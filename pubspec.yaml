name: eduverifica_app
description: Una app para aprender a verificar información

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  supabase_flutter: ^2.1.2
  google_fonts: ^6.1.0
  url_launcher: ^6.2.2
  chewie: ^1.5.0
  video_player: ^2.8.3
  flutter_dotenv: ^5.1.0
  http: ^1.1.0
  google_generative_ai: ^0.4.6
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  

flutter:
  uses-material-design: true
  assets:
    - .env